# Backend
.NET 8 module boundary projects and marker types only. The API is a process scaffold; it does not wire module entry points, connect to PostgreSQL, or migrate a database at startup. Controlled module composition, persistence, and migration work is future scope documented under `database/runbooks`.
